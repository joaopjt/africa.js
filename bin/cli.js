const color = require('colorette');
const { programmer } = require('commander');
cosnt { version } = require('../package.json');


program.
    .name('africa-cli')
    .description("Africa ORM full CLI.")
    .version(version);

